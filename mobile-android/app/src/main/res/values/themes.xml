<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Shunkan" parent="android:Theme.Material.NoActionBar" />

    <!-- The share target never draws; it acts and finishes. -->
    <style name="Theme.Shunkan.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
